filters:
  - type: "data_cleaning.annotation_standard"
    parameters:
      bounding_box_format: "XYXY"
      bounding_box_coordinate_system: "Continuous"

datasets:
  - name: "ANTIUAV"  # Anti-UAV410; must match trackit/datasets/SOT/datasets/ANTIUAV.py
    type: "SOT"
    splits:
      - "test"
